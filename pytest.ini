[pytest]
# Default run = fast unit tests only. The integration suite needs the
# docker-compose stack and is opted into explicitly:
#   pytest tests/integration
testpaths = tests/unit
markers =
    slow: long-running integration cases (Loki outage / large bursts)
